# Production Environment Variables
# Copy these to your deployment platform (Vercel, Railway, etc.)

# Required
GEMINI_API_KEY=your_actual_api_key_here
NODE_ENV=production

# Frontend URL (update with your actual frontend deployment URL)
FRONTEND_URL=https://your-app.vercel.app

# CORS Configuration (comma-separated list of allowed origins)
# Update with your actual frontend URL(s)
ALLOWED_ORIGINS=https://your-app.vercel.app,https://www.your-app.com

# Server Port (Railway/Railway-like platforms)
# Vercel handles this automatically
PORT=3001
